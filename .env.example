# ── CertiRAG Environment Configuration ─────────────────────────────
# Copy this to .env and fill in your values.

# ── Execution Mode ─────────────────────────────────────────────────
# "lite" = CPU-only / Codespaces (uses API-based models)
# "full" = GPU-accelerated (uses local HuggingFace models)
CERTIRAG_MODE=lite

# ── OpenAI API (required for lite mode, optional for full) ────────
OPENAI_API_KEY=sk-your-key-here
OPENAI_MODEL=gpt-4o-mini

# ── Paths ──────────────────────────────────────────────────────────
CERTIRAG_DATA_DIR=./data
CERTIRAG_OUTPUT_DIR=./outputs
CERTIRAG_MODEL_DIR=./models

# ── Retrieval ──────────────────────────────────────────────────────
CERTIRAG_MAX_CHUNKS=10
CERTIRAG_MAX_SPANS_PER_CHUNK=5

# ── Verification ───────────────────────────────────────────────────
CERTIRAG_TAU_ENTAIL=0.85
CERTIRAG_TAU_CONTRADICT=0.70
CERTIRAG_MAX_VERIFIER_PAIRS=240
CERTIRAG_MAX_CLAIMS=12

# ── Logging ────────────────────────────────────────────────────────
CERTIRAG_LOG_LEVEL=INFO
CERTIRAG_LOG_FORMAT=json
